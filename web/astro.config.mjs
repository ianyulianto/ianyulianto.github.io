// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://ianyulianto.github.io',
  // Default dist/ — GitHub Actions uploads this artifact to Pages
});
