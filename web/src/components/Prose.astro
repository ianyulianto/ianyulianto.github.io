<div class="prose">
  <slot />
</div>

<style>
  .prose {
    font-size: var(--body);
    line-height: var(--lh-relaxed);
    color: var(--text-2);
  }
  .prose :global(h1), 
  .prose :global(h2), 
  .prose :global(h3) {
    color: var(--text);
    line-height: var(--lh-tight);
    margin: var(--s-4) 0 var(--s-2);
  }
  .prose :global(p) { margin: 0 0 var(--s-3); }
  .prose :global(ul) { margin: 0 0 var(--s-3); padding-left: 1.2em; }
  .prose :global(code) { font-family: var(--font-mono); font-size: 0.9em; }
  .prose :global(pre) {
    font-family: var(--font-mono);
    background: var(--bg-muted);
    border: 1px solid var(--border);
    border-radius: var(--r-lg);
    padding: var(--s-3);
    overflow-x: auto;
  }
</style>
